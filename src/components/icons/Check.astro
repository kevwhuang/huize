---
interface Props {
    size?: number;
    strokeWidth?: number;
}

const { size = 20, strokeWidth = 2 } = Astro.props;
---

<svg
    aria-hidden="true"
    fill="none"
    height={size}
    stroke="currentColor"
    stroke-linecap="round"
    stroke-linejoin="round"
    stroke-width={strokeWidth}
    viewBox="0 0 24 24"
    width={size}
>
    <polyline points="20 6 9 17 4 12" />
</svg>
