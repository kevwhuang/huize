---
import { ClientRouter } from 'astro:transitions';
import { Font } from 'astro:assets';

import '@/global.css';
import '@/styles.css';
import Footer from '@sections/Footer.astro';
import Navbar from '@sections/Navbar.astro';
import { ROUTES } from '@lib/constants';

interface Props {
    description: string;
    noindex?: boolean;
    title: string;
}

const canonicalUrl = new URL(Astro.routePattern, Astro.site);
const currentRoute = ROUTES.flatMap(route => [route, ...(route.children ?? [])]).find(route => route.path === Astro.routePattern);
const { description, noindex, title } = Astro.props;
const ogImageUrl = `${Astro.site}og.png`;
const parentRoute = ROUTES.find(route => route.children?.some(child => child.path === Astro.routePattern));

const ancestorRoutes = parentRoute && parentRoute !== currentRoute ? [parentRoute] : [];

const breadcrumbRoutes = currentRoute && currentRoute !== ROUTES[0] ? [ROUTES[0], ...ancestorRoutes, currentRoute] : [];

const breadcrumbList = {
    '@type': 'BreadcrumbList',
    'itemListElement': breadcrumbRoutes.map((route, index) => ({
        '@type': 'ListItem',
        'item': new URL(route.path, Astro.site).href,
        'name': route.label,
        'position': index + 1,
    })),
};

const structuredData = {
    '@context': 'https://schema.org',
    '@graph': [
        ...(breadcrumbRoutes.length > 0 ? [breadcrumbList] : []),
        {
            '@type': 'ItemList',
            'itemListElement': ROUTES.map((route, index) => ({
                '@type': 'SiteNavigationElement',
                'name': route.label,
                'position': index + 1,
                'url': new URL(route.path, Astro.site).href,
            })),
        },
        {
            '@type': 'WebSite',
            'author': {
                '@type': 'Person',
                'name': 'Wayne Huang',
            },
            'inLanguage': 'zh-CN',
            'name': '惠泽健康',
            'url': Astro.site,
        },
    ],
};
---

<!doctype html>
<html lang="zh-CN">
    <head>
        <meta charset="utf-8" />
        <title>{title}</title>
        <meta content="Wayne Huang" name="author" />
        <meta content={description} name="description" />
        <meta content={noindex ? 'noindex, nofollow' : 'index, follow'} name="robots" />
        <meta content="#0A3A33" name="theme-color" />
        <meta content="width=device-width, initial-scale=1" name="viewport" />
        <link href="/apple-touch-icon.png" rel="apple-touch-icon" />
        <link href={canonicalUrl} rel="canonical" />
        <link href="/favicon.ico" rel="icon" type="image/x-icon" />
        <meta content={description} property="og:description" />
        <meta content={ogImageUrl} property="og:image" />
        <meta content="zh_CN" property="og:locale" />
        <meta content="惠泽健康" property="og:site_name" />
        <meta content={title} property="og:title" />
        <meta content="website" property="og:type" />
        <meta content={canonicalUrl} property="og:url" />
        <meta content="summary_large_image" name="twitter:card" />
        <meta content={description} name="twitter:description" />
        <meta content={ogImageUrl} name="twitter:image" />
        <meta content={title} name="twitter:title" />
        <script is:inline set:html={JSON.stringify(structuredData)} type="application/ld+json" />
        <Font cssVariable="--font-jost" preload />
        <Font cssVariable="--font-noto-sans-sc" preload />
        <Font cssVariable="--font-noto-serif-sc" preload />
        <ClientRouter />
    </head>
    <body class="flex flex-col overflow-x-clip min-h-svh bg-cloud antialiased text-slate">
        <Navbar />
        <main class="flex flex-1 flex-col">
            <slot />
        </main>
        <Footer />
    </body>
</html>

<script>
    import { initMotion } from '@lib/motion';

    document.addEventListener('astro:page-load', initMotion);
</script>
