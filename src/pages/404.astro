---
import Layout from '@/Layout.astro';
import NotFound from '@sections/error/NotFound.astro';
import { getPage } from '@lib/database';

const { description, title } = await getPage('404');
---

<Layout
    description={description}
    noindex
    title={title}
>
    <NotFound />
</Layout>
